# FootballTeamManagement

## Installation and Running
This project requires Node.js to run.

### Running
To run this project, use the following command on terminal: ```npm install```.

To run the Node.js Express server, use the following command on terminal: ```node server.js```.

## Testing and Coverage
To run tests and display coverage, use the following command on terminal: ```npm run coverage```.
